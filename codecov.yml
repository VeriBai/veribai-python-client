coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 2%

  ignore:
    - "tests/"
    - "examples/"

comment:
  layout: "diff, files"
  require_changes: true

github_checks:
  annotations: true
