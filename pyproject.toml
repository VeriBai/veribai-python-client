[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "veribai"
version = "0.1.0"
description = "Official Python client for the VeriBai API — VeriFactu and TicketBAI invoice submission"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{ name = "VeriBai", email = "soporte@veribai.com" }]
keywords = [
    "veribai",
    "verifactu",
    "ticketbai",
    "tbai",
    "aeat",
    "facturacion",
    "invoicing",
    "spain",
    "tax",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: Apache Software License",
    "Natural Language :: Spanish",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Office/Business :: Financial :: Accounting",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

# Deliberately minimal. This library submits legally binding fiscal records;
# every transitive dependency is supply-chain surface. `requests` only.
dependencies = ["requests>=2.25.0"]

[project.urls]
Homepage = "https://veribai.com"
Documentation = "https://veribai.com/docs/api"
Repository = "https://github.com/VeriBai/veribai-python-client"
Issues = "https://github.com/VeriBai/veribai-python-client/issues"
Changelog = "https://github.com/VeriBai/veribai-python-client/blob/main/CHANGELOG.md"

[dependency-groups]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "responses>=0.24",
    "freezegun>=1.4",
    "mypy>=1.8",
    "types-requests>=2.31",
    "ruff>=0.6",
    "bandit>=1.7",
    "pip-audit>=2.7",
]

[tool.hatch.build.targets.wheel]
packages = ["veribai"]

[tool.hatch.build.targets.sdist]
include = ["/veribai", "/tests", "/README.md", "/CHANGELOG.md", "/LICENSE"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "--strict-markers",
    "--cov=veribai",
    "--cov-report=term-missing",
    "--cov-report=xml",
]
markers = [
    "integration: talks to a real VeriBai environment (never runs in the default suite)",
]

[tool.coverage.run]
branch = true
source = ["veribai"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]

[tool.mypy]
# `python_version` matches the floor in `requires-python`, so mypy judges the code
# against the oldest interpreter we promise to run on rather than the newest.
python_version = "3.10"
strict = true
warn_unused_configs = true
disallow_untyped_defs = true
files = ["veribai"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "S", "C4", "RUF"]
ignore = [
    "S101",    # assert — used in tests
    # typing.Dict/List and Optional[X] are kept as a STYLE choice now that the floor
    # is 3.10 — the whole package uses `from __future__ import annotations`, so the
    # modern spellings would work. Dropping these four is a mechanical rewrite of
    # every signature, worth doing on its own rather than inside another change.
    "UP006",
    "UP035",
    "UP045",
    "UP007",
    "RUF002",  # en dashes and accents in prose are deliberate, not typos
    "RUF003",
    "RUF022",  # __all__ is grouped by concept, which reads better than alphabetically
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S", "E501"]

[tool.bandit]
exclude_dirs = ["tests", "examples"]
# B311 (non-cryptographic randomness): the one use is retry jitter, which spreads
# timing across workers and protects nothing. Justified inline at the call site.
skips = ["B311"]
